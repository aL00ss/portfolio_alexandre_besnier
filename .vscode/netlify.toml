[build]
  command = "npm run build"
  publish = "dist"  # Dossier de construction de votre site

[functions]
  external_node_modules = ["astro"]  # Modules Node.js marqués comme externes

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
